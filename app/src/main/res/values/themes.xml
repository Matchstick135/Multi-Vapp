<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.Vapp" parent="android:Theme.Light.NoTitleBar">
        <item name="android:statusBarColor">@color/black</item>
        <item name="android:windowBackground">@color/white</item>
    </style>

    <style name="Theme.Vapp.NoActionBar" parent="Theme.Vapp">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.Vapp.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.Vapp.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>